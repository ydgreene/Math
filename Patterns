echo "# Math" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/ydgreene/Math.git
git push -u origin master
import math
import sys
a = int(sys.argv[1])
b = int(sys.argv[2])
c = int(sys.argv[3])
d = int(sys.argv[4])
n = 0

while a, b, c, d >= 0:
    e = n ** 5
    a ** 5
    b ** 5
    c ** 5
    d ** 5
    if a + b + c + d == e:
            print(e)

